require("leetcode").setup({
	lang = "python3",
	-- it otherwise works fine but the issue is that for some reason images stay there even if i switch to a different tmux window
	-- might be my fault TODO look into this later
	image_support = false,
})
